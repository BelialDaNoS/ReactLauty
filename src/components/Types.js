import { Card, Row } from "react-bootstrap";

const Types = ({type}) =>{
    console.log("hola")
    return (
        <Row>
        {/* {type.map((item)=>
            <h2>{item.type.name}</h2>
        )} */}
        </Row>
    )
}
  export default Types;